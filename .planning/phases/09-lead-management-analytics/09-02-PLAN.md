---
phase: 09-lead-management-analytics
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - packages/admin/src/api.ts
  - packages/admin/src/pages/LeadList.tsx
  - packages/admin/src/pages/EditCompany.tsx
  - packages/admin/src/components/StatsPanel.tsx
  - packages/admin/src/App.tsx
  - packages/admin/src/styles/admin.css
autonomous: false
requirements: [LEAD-01, LEAD-02, STATS-01]

must_haves:
  truths:
    - "Admin can view a paginated list of leads with name, email, phone, material, estimate range, and date"
    - "Admin can type a search term and the lead list filters by name or email"
    - "Admin can select a date range and the lead list filters to that range"
    - "Admin can click an Export CSV button and a CSV file downloads"
    - "Admin sees a stats panel with total leads, total estimates, popular material, and average sqft"
    - "Leads tab and stats panel are accessible from the company edit page"
  artifacts:
    - path: "packages/admin/src/pages/LeadList.tsx"
      provides: "Lead list page with search, date filter, pagination, and CSV export"
      min_lines: 80
    - path: "packages/admin/src/components/StatsPanel.tsx"
      provides: "Stats dashboard panel showing aggregate metrics"
      min_lines: 30
    - path: "packages/admin/src/api.ts"
      provides: "API client functions for leads, CSV export, and stats"
      contains: "getLeads|exportLeadsCsv|getStats"
  key_links:
    - from: "packages/admin/src/pages/LeadList.tsx"
      to: "packages/admin/src/api.ts"
      via: "api.getLeads() with search/date params"
      pattern: "api\\.getLeads"
    - from: "packages/admin/src/pages/LeadList.tsx"
      to: "packages/admin/src/api.ts"
      via: "api.exportLeadsCsv() triggers download"
      pattern: "api\\.exportLeadsCsv"
    - from: "packages/admin/src/components/StatsPanel.tsx"
      to: "packages/admin/src/api.ts"
      via: "api.getStats() on mount"
      pattern: "api\\.getStats"
    - from: "packages/admin/src/pages/EditCompany.tsx"
      to: "packages/admin/src/pages/LeadList.tsx"
      via: "Leads tab renders LeadList component"
      pattern: "LeadList"
---

<objective>
Build the admin frontend for lead management and analytics: a searchable/filterable leads table with CSV export, and a stats dashboard panel.

Purpose: Gives admins visibility into their leads and business metrics directly from the admin portal.
Output: LeadList page, StatsPanel component, and updated EditCompany with new tabs.
</objective>

<execution_context>
@/home/mike/.claude/get-shit-done/workflows/execute-plan.md
@/home/mike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-lead-management-analytics/09-01-SUMMARY.md

<interfaces>
<!-- API endpoints created in Plan 01 (to be consumed here): -->

<!-- GET /admin/companies/:companyId/leads?search=X&from=YYYY-MM-DD&to=YYYY-MM-DD&page=N&pageSize=N -->
<!-- Returns: { data: Lead[], total: number, page: number, pageSize: number } -->
<!-- Lead shape: { id, companyId, firstName, lastName, email, phone, consentGiven, consentText, sqft, pitch, material, estimateLow, estimateHigh, address, createdAt } -->

<!-- GET /admin/companies/:companyId/leads/csv?search=X&from=YYYY-MM-DD&to=YYYY-MM-DD -->
<!-- Returns: text/csv with Content-Disposition: attachment -->

<!-- GET /admin/companies/:companyId/stats -->
<!-- Returns: { totalLeads: number, totalEstimates: number, popularMaterial: string|null, averageSqft: number } -->

<!-- From packages/admin/src/api.ts existing pattern: -->
```typescript
async function request(path: string, options?: RequestInit): Promise<Response>;
async function jsonRequest(path: string, options?: RequestInit): Promise<Response>;
export const api = { login, logout, listCompanies, ... };
```

<!-- From packages/admin/src/pages/EditCompany.tsx: -->
```typescript
type Tab = 'branding' | 'pricing' | 'embed';
const TABS: { key: Tab; label: string }[] = [ ... ];
export function EditCompany({ companyId }: { companyId: string }) { ... }
```

<!-- Existing admin CSS classes (from inspecting components): -->
<!-- .card, .page-header, .page-title, .page-subtitle, .tab-bar, .tab-btn, .tab-btn--active, .tab-content -->
<!-- .form-group, .form-label, .form-input, .btn, .btn-primary -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API client functions and build LeadList page with search, filters, pagination, and CSV export</name>
  <files>packages/admin/src/api.ts, packages/admin/src/pages/LeadList.tsx, packages/admin/src/pages/EditCompany.tsx, packages/admin/src/styles/admin.css</files>
  <action>
    1. In packages/admin/src/api.ts, add three new methods to the api object:

       - `getLeads(companyId: string, params?: { search?: string; from?: string; to?: string; page?: number; pageSize?: number })` — builds query string from params, calls GET /companies/{companyId}/leads?..., returns typed { data: Lead[], total, page, pageSize }. Define a Lead interface matching the DB schema fields (id, firstName, lastName, email, phone, sqft, pitch, material, estimateLow, estimateHigh, address, createdAt).

       - `exportLeadsCsv(companyId: string, params?: { search?: string; from?: string; to?: string })` — calls GET /companies/{companyId}/leads/csv?..., gets the response blob, creates a temporary element with URL.createObjectURL, triggers download, revokes URL. Uses the request() function (not jsonRequest) since response is CSV.

       - `getStats(companyId: string)` — calls GET /companies/{companyId}/stats, returns typed { totalLeads, totalEstimates, popularMaterial, averageSqft }.

    2. Create packages/admin/src/pages/LeadList.tsx:
       - Props: { companyId: string }
       - State: leads array, total count, current page, search text (debounced input), fromDate, toDate, loading boolean
       - On mount and when search/dates/page change: call api.getLeads with current params
       - Debounce search input by 300ms (use setTimeout/clearTimeout pattern, no external lib)
       - Render:
         a. Search bar: text input with placeholder "Search by name or email..."
         b. Date filters: two date inputs (From / To) side by side
         c. Export CSV button: calls api.exportLeadsCsv with current search/date filters
         d. Leads table with columns: Name (firstName lastName), Email, Phone, Material, Estimate ($low - $high formatted), Date (formatted createdAt)
         e. If address present, show it in a secondary line under the name
         f. Pagination controls: Previous/Next buttons with "Page X of Y" text, disabled when at bounds
         g. Empty state: "No leads yet" message when total is 0
         h. Loading state: show "Loading..." when fetching
       - Format currency with Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', maximumFractionDigits: 0 })
       - Format dates with toLocaleDateString()

    3. Modify packages/admin/src/pages/EditCompany.tsx:
       - Import LeadList from './LeadList'
       - Expand Tab type to: 'branding' | 'pricing' | 'embed' | 'leads'
       - Add { key: 'leads', label: 'Leads' } to TABS array (after 'embed')
       - Add rendering: `{activeTab === 'leads' && <LeadList companyId={companyId} />}`

    4. Add CSS to packages/admin/src/styles/admin.css for the lead list:
       - .lead-filters: flex row, gap 12px, wrap on mobile, margin-bottom 16px
       - .lead-filters input: use existing .form-input styling
       - .lead-table: width 100%, border-collapse, basic table styling (header bold, row borders, padding 8-12px)
       - .lead-table th: background #f8fafc, text-align left
       - .lead-table td: border-bottom 1px solid #e5e7eb
       - .lead-address: font-size 12px, color #64748b
       - .pagination: flex row, justify-content center, align-items center, gap 12px, margin-top 16px
       - .export-btn: style similar to .btn but secondary (outline style)
       - Responsive: on screens < 768px, make table horizontally scrollable (overflow-x: auto wrapper)
  </action>
  <verify>
    <automated>cd packages/admin && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - LeadList page renders with search, date filters, pagination, and CSV export button
    - EditCompany has a "Leads" tab that renders LeadList
    - All TypeScript compiles without errors
    - API client functions correctly build query strings and handle responses
  </done>
</task>

<task type="auto">
  <name>Task 2: Build StatsPanel component and wire into company edit page</name>
  <files>packages/admin/src/components/StatsPanel.tsx, packages/admin/src/pages/EditCompany.tsx, packages/admin/src/styles/admin.css</files>
  <action>
    1. Create packages/admin/src/components/StatsPanel.tsx:
       - Props: { companyId: string }
       - State: stats object (totalLeads, totalEstimates, popularMaterial, averageSqft), loading boolean
       - On mount: call api.getStats(companyId), set stats
       - Render a grid of 4 stat cards:
         a. "Total Leads" — stats.totalLeads (number)
         b. "Total Estimates" — stats.totalEstimates (number)
         c. "Popular Material" — stats.popularMaterial or "N/A" (capitalize first letter of each word)
         d. "Average Sq Ft" — stats.averageSqft formatted with commas (Intl.NumberFormat)
       - Each card: icon-less, just a label (small gray text above) and value (large bold text below)
       - Loading state: 4 skeleton cards
       - Empty state (all zeros): show cards with "0" / "N/A" values — no special empty state needed

    2. Modify packages/admin/src/pages/EditCompany.tsx:
       - Import StatsPanel
       - Add 'stats' to Tab type: 'branding' | 'pricing' | 'embed' | 'leads' | 'stats'
       - Add { key: 'stats', label: 'Stats' } to TABS array (after 'leads')
       - Render: `{activeTab === 'stats' && <StatsPanel companyId={companyId} />}`

    3. Add CSS to packages/admin/src/styles/admin.css:
       - .stats-grid: display grid, grid-template-columns repeat(auto-fit, minmax(180px, 1fr)), gap 16px
       - .stat-card: background white, border 1px solid #e5e7eb, border-radius 8px, padding 20px, text-align center
       - .stat-label: font-size 13px, color #64748b, margin-bottom 4px, text-transform uppercase, letter-spacing 0.5px
       - .stat-value: font-size 28px, font-weight 700, color #1e293b
  </action>
  <verify>
    <automated>cd packages/admin && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - StatsPanel shows 4 metric cards with data from the stats API
    - EditCompany has a "Stats" tab that renders StatsPanel
    - TypeScript compiles without errors
    - Stats panel handles loading and zero-data states gracefully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify lead management UI and stats dashboard</name>
  <files>packages/admin/src/pages/LeadList.tsx, packages/admin/src/components/StatsPanel.tsx, packages/admin/src/pages/EditCompany.tsx</files>
  <action>
    Human verification checkpoint. All code was built in Tasks 1 and 2. This task verifies the complete UI works end-to-end by manual testing in the browser.
  </action>
  <verify>
    <automated>cd packages/admin && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>User confirms: lead list displays correctly with search/filter/export, stats panel shows correct metrics, both tabs work on company edit page.</done>
</task>

</tasks>

<verification>
- `cd packages/admin && npx tsc --noEmit` — no TypeScript errors
- `cd packages/api && npm test` — all tests pass
- Admin leads tab shows searchable, filterable, paginated lead list
- CSV export downloads correct data
- Stats tab shows aggregate metrics
</verification>

<success_criteria>
Admin users can:
1. View leads in a paginated table with search and date filtering (LEAD-01)
2. Export leads as CSV (LEAD-02)
3. See per-company stats with totals, popular material, and avg sqft (STATS-01)
All accessible from the company edit page via new tabs.
</success_criteria>

<output>
After completion, create `.planning/phases/09-lead-management-analytics/09-02-SUMMARY.md`
</output>
