---
phase: 10-admin-widget-polish
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - packages/admin/src/api.ts
  - packages/admin/src/pages/CompanyList.tsx
  - packages/admin/src/pages/EditCompany.tsx
  - packages/admin/src/components/BrandingSettings.tsx
  - packages/admin/src/components/PricingSettings.tsx
  - packages/admin/src/styles/admin.css
autonomous: false
requirements: [COMP-01, COMP-02, PRICE-01]

must_haves:
  truths:
    - "Admin can click Archive on a company row and it disappears from the active list"
    - "Admin can toggle to show archived companies and see them with a visual indicator"
    - "Admin can restore an archived company back to the active list"
    - "In the branding editor, changing logo or color immediately updates a widget preview panel"
    - "Pricing form shows inline validation errors when cost_low >= cost_high, negative values, or out-of-range values"
  artifacts:
    - path: "packages/admin/src/api.ts"
      provides: "archiveCompany, restoreCompany API methods"
      contains: "archiveCompany"
    - path: "packages/admin/src/pages/CompanyList.tsx"
      provides: "Archive/restore buttons, archived toggle, visual indicator"
    - path: "packages/admin/src/components/BrandingSettings.tsx"
      provides: "Live widget preview iframe or inline preview component"
      contains: "preview"
    - path: "packages/admin/src/components/PricingSettings.tsx"
      provides: "Client-side validation with inline error messages"
      contains: "error"
  key_links:
    - from: "packages/admin/src/pages/CompanyList.tsx"
      to: "/api/admin/companies/:id/archive"
      via: "api.archiveCompany(id)"
      pattern: "archiveCompany"
    - from: "packages/admin/src/components/BrandingSettings.tsx"
      to: "widget preview"
      via: "iframe src pointing to company slug URL or inline preview div"
      pattern: "iframe|preview"
---

<objective>
Add archive/restore UI to company list, live widget preview to branding editor, and client-side pricing validation.

Purpose: COMP-01 UI lets admins archive/restore companies. COMP-02 gives admins a real-time widget preview as they change branding. PRICE-01 client-side validation provides immediate feedback before the API rejects invalid pricing.
Output: Updated CompanyList with archive controls, BrandingSettings with live preview, PricingSettings with inline validation.
</objective>

<execution_context>
@/home/mike/.claude/get-shit-done/workflows/execute-plan.md
@/home/mike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-admin-widget-polish/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — archive/restore endpoints -->
API endpoints added by 10-01:
- PATCH /api/admin/companies/:companyId/archive -> { success: true }
- PATCH /api/admin/companies/:companyId/restore -> { success: true }
- GET /api/admin/companies?includeArchived=true -> includes archivedAt field

From packages/admin/src/api.ts:
```typescript
export interface Company {
  id: string;
  name: string;
  slug: string | null;
  email?: string;
  logoUrl: string | null;
  primaryColor: string;
}
export const api = { /* methods */ };
```

From packages/admin/src/pages/CompanyList.tsx:
```typescript
export function CompanyList() { /* renders company table with Edit action */ }
```

From packages/admin/src/components/BrandingSettings.tsx:
```typescript
export function BrandingSettings({ companyId }: { companyId?: string }) {
  const [logoUrl, setLogoUrl] = useState<string | null>(null);
  const [primaryColor, setPrimaryColor] = useState('#2563eb');
  // ... logo upload, color picker with debounced save
}
```

From packages/admin/src/components/PricingSettings.tsx:
```typescript
interface PricingRow {
  materialKey: string;
  costLow: number | null;
  costHigh: number | null;
  pitchFlat: number | null; pitchLow: number | null;
  pitchMedium: number | null; pitchSteep: number | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add archive/restore to CompanyList and pricing validation to PricingSettings</name>
  <files>packages/admin/src/api.ts, packages/admin/src/pages/CompanyList.tsx, packages/admin/src/components/PricingSettings.tsx, packages/admin/src/styles/admin.css</files>
  <action>
1. In api.ts:
   - Add `archivedAt?: string | null` to the Company interface.
   - Add `archiveCompany(companyId: string)` method: PATCH to `/companies/${companyId}/archive` using jsonRequest.
   - Add `restoreCompany(companyId: string)` method: PATCH to `/companies/${companyId}/restore` using jsonRequest.
   - Update `listCompanies` to accept optional `{ includeArchived?: boolean }` param. If includeArchived, append `?includeArchived=true` to the request URL.

2. In CompanyList.tsx:
   - Add `showArchived` toggle state (boolean, default false).
   - Pass `{ includeArchived: showArchived }` to `api.listCompanies()`. Re-fetch when toggle changes.
   - Add a "Show archived" checkbox/toggle above the table.
   - Add an "Archive" button in the Actions column for each non-archived company (confirm with window.confirm before calling api.archiveCompany). On success, remove from list (or re-fetch).
   - When showArchived is true, archived companies show with a dimmed row style and an "Archived" badge next to the name, plus a "Restore" button instead of "Archive".
   - Add CSS classes: `.company-archived` (opacity 0.6), `.badge-archived` (small red/muted badge).

3. In PricingSettings.tsx:
   - Add a `validationErrors` state: `Record<string, string>` keyed by `${materialKey}-${field}`.
   - Add a `validatePricing(rows: PricingRow[]): Record<string, string>` function that checks:
     - No negative costLow/costHigh values
     - costLow < costHigh when both are set (error: "Low must be less than high")
     - costLow and costHigh <= 100 (error: "Must be under $100/sqft")
     - No negative pitch multipliers
     - Pitch multipliers <= 5.0 (error: "Must be under 5.0")
   - Call validatePricing on every field change (updateField). Display inline error below each input using a `.field-error` CSS class (red text, small font).
   - In handleSave, run validatePricing first. If errors exist, do NOT call API and show status message "Please fix validation errors".
   - Add CSS: `.field-error { color: var(--red-400, #f87171); font-size: 0.75rem; margin-top: 2px; }`
  </action>
  <verify>
    <automated>cd /home/mike/roofing_calculator/packages/admin && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <done>CompanyList shows archive/restore controls with toggle for archived companies. PricingSettings validates inputs inline with clear error messages. Admin build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add live widget preview to BrandingSettings</name>
  <files>packages/admin/src/components/BrandingSettings.tsx, packages/admin/src/styles/admin.css</files>
  <action>
1. In BrandingSettings.tsx, add a widget preview panel below the color picker card:
   - Create a new card with header "Widget Preview" and description "Live preview of your calculator widget".
   - Inside, render an inline preview div (NOT an iframe -- the widget CSS/JS would be heavy to load in an iframe and cause cross-origin issues in dev). Instead, create a styled mock preview that shows:
     - A header bar with the company logo (from `logoUrl` state) and the brand color (from `primaryColor` state) as the background/accent.
     - A mock step indicator (3 dots, first active in primaryColor).
     - A title "Get Your Roof Estimate" and subtitle "Tell us about your roof".
     - A mock form area with placeholder input fields (non-interactive, just for visual).
     - A mock "Continue" button styled with `primaryColor` as background.
   - This preview updates instantly as `logoUrl` or `primaryColor` changes (they are already reactive state).
   - Style the preview container: `max-width: 360px`, card background matching widget's white/light style, rounded corners, subtle shadow. Use inline styles with `primaryColor` for the accent elements.

2. Add CSS in admin.css:
   - `.widget-preview` container: `max-width: 360px; background: #fff; border-radius: 12px; overflow: hidden; box-shadow: 0 2px 12px rgba(0,0,0,0.15);`
   - `.widget-preview-header`: padding, centered text
   - `.widget-preview-pip`: small dots for step indicator
   - `.widget-preview-body`: padding, mock form fields
   - `.widget-preview-btn`: full-width button placeholder
   - `.widget-preview-input`: mock input field appearance (border, border-radius, gray background)
  </action>
  <verify>
    <automated>cd /home/mike/roofing_calculator/packages/admin && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <done>Branding editor shows a live widget preview that updates logo and color in real time as admin changes them. Admin build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify admin UI polish features</name>
  <files>packages/admin/src/pages/CompanyList.tsx, packages/admin/src/components/BrandingSettings.tsx, packages/admin/src/components/PricingSettings.tsx</files>
  <action>Human verification of archive/restore UI, live branding preview, and pricing validation. Start the admin dev server and test each feature interactively.</action>
  <verify>
    <automated>cd /home/mike/roofing_calculator/packages/admin && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms: archive/restore works, widget preview updates live, pricing validation shows errors inline</done>
  <what-built>Company archive/restore UI, live branding preview, and pricing validation</what-built>
  <how-to-verify>
    1. Start the admin dev server: `cd packages/admin && npx vite`
    2. Log in as super-admin
    3. On Companies page: click "Archive" on a company, confirm it disappears. Toggle "Show archived" to see it dimmed with badge. Click "Restore" to bring it back.
    4. Go to a company's Branding tab: change the color picker -- verify the widget preview below updates its accent color instantly. Upload a new logo -- verify preview shows it.
    5. Go to Pricing tab: enter cost_low=5, cost_high=3 -- verify inline error "Low must be less than high". Enter a negative value -- verify error. Enter cost > 100 -- verify error. Click Save with errors -- verify it does not submit.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Admin builds successfully with no TypeScript errors
- Archive/restore flow works end-to-end (API -> UI)
- Widget preview updates reactively with logo and color changes
- Pricing validation catches all invalid inputs with clear messages
</verification>

<success_criteria>
- Companies can be archived and restored from the admin UI
- Archived companies are hidden by default but viewable with toggle
- Branding editor has a live widget preview panel
- Pricing form has inline validation preventing invalid inputs
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-widget-polish/10-02-SUMMARY.md`
</output>
